import React from "react";
import ReactDOM from "react-dom";
import Records from "./components/Records";
import "./styles.css";

const rootElement = document.getElementById("root");
ReactDOM.render(<Records />, rootElement);
